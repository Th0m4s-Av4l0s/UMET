#Operador "=" asigna el valor de la variable
# derecha a la de la izquierda (x <- 5)
 
#Operador "+=" el valor de la variable derecha
# suma al valor de la variable izquierda
# (x(10) += 5 -> X(15))

#Operador "-=" el valor de la variable derecha
# resta el valor de la variable izquierda
#(X(15) -= 5 -> X(10))

#Operador "*=" el valor de la variable deracha
# multiplica el valor de la variable izquierda
#(X(3) *= 5 -> X(15))

#Operador "/=" el valor de la variable derecha
# divide el valor de la variable izquierda
#(X(10) /= 3 -> 10 / 3 = 3.3 -> X(3.3))

#Operador "//=" el valor de la variable derecha
# divide de forma entera el valor de la 
# variable izquierda
# (X(10) //= 3 -> 10 // 3 = 3 = X(3))

#Operador "**=" el valor de la variable derecha
# potencia el valor de la variable izquierda
# (X(2) **= 3 -> X(16))

#Operador "%=" el valor de la variable derecha
# Divide y deja el resto de la division
# en la variable izquierda
# (X(10) %= 2 -> 10 % 2 = 0 -> X(0))