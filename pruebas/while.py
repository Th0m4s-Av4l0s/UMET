# Iteracion = repeticion de una parte del codigo
# dentro del propio programa
# Ciclo while ejemplo:

x = 0

while(x <= 5):
    print(x);
    x += 1
    if(x == 3):
        break;

print("Fin.");

#While siempre se evalua al principio por lo 
# que puede no llegar a ejecutarse

